{
  "name": "misc-helpers",
  "version": "1.3.16",
  "author": "Arthur José Germano",
  "license": "MIT",
  "main": "./index.js",
  "scripts": {
    "test": "vitest --run --reporter verbose --globals",
    "testWebAuthn2": "vitest --run --reporter verbose --globals ./tests/webauthn2.test.js",
    "testWebAuthn3": "vitest --run --reporter verbose --globals ./tests/webauthn3.test.js",
    "testBulkProcessor": "vitest --run --reporter verbose --globals ./tests/bulkProcessor.test.js"
  },
  "keywords": [
    "functions",
    "helpers",
    "validators"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/arthurgermano/misc-helpers.git"
  },
  "bugs": {
    "url": "https://github.com/arthurgermano/misc-helpers/issues"
  },
  "dependencies": {
    "cbor-x": "^1.5.9",
    "date-fns": "^3.6.0",
    "fflate": "^0.8.2",
    "lodash.clonedeep": "^4.5.0"
  },
  "devDependencies": {
    "vite": "^5.2.8",
    "vitest": "^1.4.0"
  },
  "homepage": "https://github.com/arthurgermano/misc-helpers#readme",
  "description": "Several functions that are always needed in projects",
  "directories": {
    "test": "tests"
  }
}
